# 🏁 Whisper GPU 백엔드 배포 요약 (프론트엔드 연동용)

백엔드 작업을 성공적으로 마치고 클라우드로 배포를 완료했습니다. 프론트엔드 작업 시 참고하세요.

## 🚀 1. 서버 주소 및 엔드포인트
모든 주소는 HTTPS/WSS 보안 프로토콜을 사용해야 합니다.

*   **API 베이스 주소**: `https://ieum-stt.livelymushroom-0e97085f.australiaeast.azurecontainerapps.io`
*   **실시간 웹소켓(WS)**: `wss://ieum-stt.livelymushroom-0e97085f.australiaeast.azurecontainerapps.io/ws`
*   **음성 조각 업로드 (POST)**: `/chunk`
    *   예: `https://.../chunk` (주의: `upload-chunk` 아님)
*   **회의 종료 (POST)**: `/end`
*   **서버 상태 확인**: `https://.../result`

## ✅ 2. 백엔드 구축 현황
*   **이미지**: `whispergpu-final:latest` (5.77GB)
*   **인프라**: Azure Container Apps (`ieum-stt`)
*   **자원**: NC 시리즈 (GPU: **Tesla T4**, 8 vCPU, 56GB RAM) 할당 완료
*   **의존성**: PyTorch 2.4.0 + cuDNN 9 + Pyannote 3.3.1 (정상 가동 중)

## 🛠️ 3. 프론트엔드 수정 가이드
1.  프론트엔드 코드 내의 API 서버 주소를 위 **Azure 주소**로 바꿉니다.
2.  전송 엔드포인트를 `/chunk`로 정확히 맞춥니다.
3.  브라우저에서 `Mixed Content` 에러 방지를 위해 프런트엔드 앱도 가급적 `https` 환경에서 테스트하세요.

## 📋 4. 테스트 확인
*   로컬 터미널에서 `python ws_client.py`를 실행하면 클라우드 서버와 실시간 연결 여부를 즉시 확인할 수 있습니다.
*   서버 로그 확인 명령어: `az containerapp logs show -n ieum-stt -g 8ai-2nd-team4 --follow`
